This is a stub types definition for @types/serialport (https://github.com/serialport/node-serialport#readme).

serialport provides its own type definitions, so you don't need @types/serialport installed!